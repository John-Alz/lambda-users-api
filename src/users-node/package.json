{
  "name": "users-node",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc && cp package*.json dist/ && cd dist && npm install --production"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/node": "^24.3.1",
    "serverless-esbuild": "^1.55.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.882.0",
    "@aws-sdk/client-sns": "^3.882.0",
    "@aws-sdk/client-sqs": "^3.882.0",
    "@aws-sdk/lib-dynamodb": "^3.882.0",
    "uuid": "^11.1.0"
  }
}
